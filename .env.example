# API Configuration
# In development, defaults to http://localhost:2412
# In production, defaults to window.location.origin
VITE_API_URL=http://localhost:2412
PORT=2412
MAX_SCORES=50
FRONTEND_URL=http://localhost:5173
NODE_ENV=production

# Reverse Proxy Configuration
# Only needed when running behind a reverse proxy (nginx, Cloudflare, Traefik, etc.)
# ⚠️ Leave commented out for direct connections (default: no proxy)

# TRUST_PROXY=true
# REAL_IP_HEADER=cf-connecting-ip

# Common proxy configurations:
# - Cloudflare:        REAL_IP_HEADER=cf-connecting-ip
# - nginx/Apache:      REAL_IP_HEADER=x-real-ip  or  REAL_IP_HEADER=x-forwarded-for
# - Direct connection: Leave both commented out (secure default)