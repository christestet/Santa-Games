services:
  santa-game:
    build: .
    container_name: santa-game
    ports:
      - "${PORT}:${PORT}"
    env_file:
      - .env
    volumes:
      - ./scores.json:/app/scores.json
    restart: unless-stopped
