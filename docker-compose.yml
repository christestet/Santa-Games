services:
  santa-game:
    image: christmas-santa-game:latest
    build: .
    container_name: santa-game
    ports:
      - "${PORT:-2412}:${PORT:-2412}"
    env_file:
      - .env
    volumes:
      - santa_data:/app/data
    restart: unless-stopped

volumes:
  santa_data:
